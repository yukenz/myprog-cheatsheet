xs:schema xmlns -> namespace xs <- URL
xs:schema xmlns:xs="URL"

xs:element name -> method
    xs:complexType
        xs:sequence
            xs:element name, type -> xs:typeData
            
xs:complexType name -> entity
    xs:sequence
        xs:element name, type -> xs:typeData
        
xs:simpleType
    xs:restriction base -> xs:typeData
        xs:enumeration value